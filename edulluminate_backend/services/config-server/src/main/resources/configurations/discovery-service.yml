eureka:
  instance:
    hostname: localhost
  client:
    registerWithEureka: false
    #    or register-with-eureka
    fetchRegistry: false
    #    or fetch-registry
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}/${server.port}/eureka/
server:
  port: 8761